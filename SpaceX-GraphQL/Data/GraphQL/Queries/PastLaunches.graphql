query PastLaunches($limit: Int!) {
  launchesPast(limit: $limit) {
    id
    details
    mission_name
    launch_date_local
    launch_site {
      site_name_long
      site_id
    }
    rocket {
      rocket_name
    }
  }
}
